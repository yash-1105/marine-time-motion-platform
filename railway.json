{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ \"${SERVICE_ROLE:-api}\" = \"worker\" ]; then exec dramatiq apps.worker.main --processes 1 --threads 4; else alembic upgrade head && python seed.py && exec uvicorn apps.api.main:app --host 0.0.0.0 --port ${PORT:-8000}; fi'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
