# Assumptions and Decisions Register

| ID | Spec reference | Ambiguity | Decision taken | Configurable? | Config key | Test |
|---|---|---|---|---|---|---|
| A-001 | §6 | Port Stay convention (ETA-to-ATD vs ATA-to-ATD) | Default to ATA-to-ATD | Yes | `metrics.port_stay.start_event` | `test_port_stay_calculation` |
| A-002 | §6 | Turnaround exclusions (litigation, repairs) | Default none, admin-configurable exclusion list | Yes | `metrics.turnaround.exclusions` | `test_turnaround_exclusions` |
| A-003 | §6 | Restow treatment in TEU throughput | Default excluded from throughput, reported separately | Yes | `metrics.throughput.include_restow` | `test_teu_throughput_restow` |
| A-004 | §6 | Auto-merge threshold | Default 0.98, review band 0.85–0.9799 | Yes | `identity.merge.threshold` | `test_auto_merge_thresholds` |
| A-005 | Legacy §6 (superseded by FRD v2 §4.4.6) | Outlier method | Retired the legacy global MAD default. FRD v2 now governs P90/P30 defaults plus explicit P95 and mean+3 sample-SD overrides through the versioned rule registry. | Yes | `config/outlier_rules.yaml` | `test_v2_outlier_registry_and_threshold_methods` |
| A-006 | §6 | Criticality weighting | Default arithmetic mean of the three component scores | Yes | `analytics.criticality.method` | `test_criticality_weighting` |
| A-007 | §6 | Tug-before-pilot tolerance | Default treated as violation, port-level override available | Yes | `rules.sequence.tug_before_pilot.tolerance` | `test_sequence_violation_tug` |
| A-008 | §9 | Canonical occurrence selection precedence when multiple observations conflict | Verification status first, then confidence, then configured source priority; ties broken by earliest ingested | Yes | `thresholds.canonical_occurrence_selection` | `test_dq010_conflicting_ata_preserved_and_resolved` |
| A-009 | §9 | Active/wait/hold/delay/unclassified decomposition must not double-count overlapping stages (e.g. shifting occurs inside Cargo Operations) | Compute the main-path sequential stages first (they partition the timeline by construction), then carve the shifting duration out of whichever main-path bucket contains it and add it to DELAY | No (algorithmic) | n/a | `test_shifting_time_is_carved_out_of_cargo_not_double_counted` |
| A-010 | §9 | AI journey narrative must never fabricate a fact; Gemini API key is not provisioned in this environment | Build a fact sheet strictly from persisted reconstruction rows; try Gemini only if `GEMINI_API_KEY` is configured, otherwise generate deterministically from the same fact sheet so the feature is fully functional offline | Yes | `GEMINI_API_KEY`, `GEMINI_MODEL` | `test_narrative_is_grounded_and_does_not_invent_missing_stage` |
| A-011 | §9 | "Missing" vs "inferred" events: no inference source exists yet in the pipeline (no phase has built one) | Every currently-absent single-occurrence event is reported MISSING, never INFERRED, on this fixture; the `is_inferred`/`inference_reason` fields and code path exist and are exercised so a future inference rule (e.g. Phase 07+) can populate them without a schema change | Yes (per-rule) | n/a | `test_missing_ata_produces_unavailable_stage_not_zero_duration` |
| A-012 | §10.3 | Percentile interpolation method | Explicit linear interpolation via Polars `pl.Series.quantile(..., interpolation="linear")`; disclosed on every result envelope | Yes | `analytics.percentiles.method` | `test_statistics_percentile_method_is_linear_interpolation` |
| A-013 | §10.1 | Negative execution delay treatment | Valid early service delivery, preserved with sign; never zeroed, never absolute-valued | Yes | `analytics.delay.allow_early_service` | `test_negative_execution_delay_preserved_as_early_service` |
| A-014 | §10.2 | Missing source data metrics (yard, crane moves, gate, lashing) | Registered explicitly as `NO_SOURCE_DATA` with stated required-event list; returns UNAVAILABLE without fabricating values | No (governed) | n/a | `test_no_source_data_metrics_registered_unavailable` |
| A-016 | §11 | Duplicate KPI concepts (11/53 tug response, 14/51 berth occupancy) | Designated primary/alias pairs; aliases excluded from default scorecards to prevent double-counting; admin endpoint allows primary designation swap | Yes | `kpi.primary_selection` | `test_alias_pairs_primary_and_alias_status` |
| A-017 | §11 | Unit isolation for KPI-02 (Average Vessel Call Size) | Segmented strictly by unit (TEU, MT, Units); unqualified mixed-unit totals prohibited | Yes | `kpi.unit_segmentation` | `test_computable_kpis_arithmetic_on_hand_verified_fixture` |
| A-018 | §11 | Peer benchmark absence handling | When peer data is unconfigured, source and period are empty and absent notice is returned rather than inventing numbers | No (governed) | n/a | `test_benchmark_peer_data_absence_disclosed` |
| A-019 | §13 | Incident/resource/towage values are absent from the current governed dashboard result | Daily Operations renders `UNAVAILABLE` with a required-input reason; it never queries or computes reporting-only values | No (governed) | n/a | `test_report_unavailable_sections` |
| A-020 | §14 | Sarvam credential may be absent in local/test environments | The copilot uses deterministic server-side selection of the same fixed governed tool surface; it returns only tool results and labels provider configuration. No model-memory answer is emitted. | Yes | `SARVAM_API_KEY`, `SARVAM_MODEL` | `test_copilot_core` |
| A-021 | §§10.3, 12.3 | The KPI scorecard is an aggregate catalogue while percentile distributions are defined for eligible lead-time populations | Surface P75/P90 from the existing persisted `analytics.statistical_aggregate` results in the Governed KPIs experience; the API remains the sole percentile calculator and declares the configured linear-interpolation method. | Yes | `analytics.percentiles.method` | `test_governed_statistics_expose_p75_and_p90` |
| A-022 | §§10.2, 10.3, 11, 12.3 | “Service Line KPI” is not a specified KPI or source field | Interpret it as a governed Service Type performance view, because the supplied ServiceRequest model and source `Services.Service_Type` provide that actual dimension. It reports mean execution delay (`served − scheduled`) by service type and movement, with count as denominator and record lineage; it is not added as a fabricated numbered KPI. Shipping Line remains a separate specified aggregation/filter dimension. | No (source-driven) | n/a | `test_service_line_view_uses_service_type_and_real_execution_records` |
| A-023 | §§7, 16, 20 | Upload analytics can exceed a browser/proxy request window | Persist raw/staging/canonical work synchronously, then enqueue the existing durable Redis/Dramatiq worker to run governed downstream analytics. The batch remains `PROCESSING` until the worker atomically activates it after success; failures remain queryable with an error message. | No (durable execution requirement) | n/a | `test_dataset_upload_queues_analytics_and_worker_commits_batch` |
| A-024 | §§12, 16, 20 | The specification governs functionality and accessibility but does not prescribe a detailed visual language | Retain the existing teal maritime identity and information architecture; centralize the presentation in shared tokens/primitives and use a consistent outline-icon system. Existing route behavior, API calls, controls, and governed status semantics remain unchanged. | No (presentation only) | n/a | frontend lint, type check, production build, route smoke checks |
| A-025 | §§12, 20 | Sidebar routes currently repeat the route title in both the application shell and page body | Treat the application-shell heading as the single primary page heading. The shared body `PageHeader` becomes a compact metadata/action toolbar when controls exist and renders nothing otherwise; record-specific context remains visible as toolbar metadata. | No (presentation only) | n/a | frontend lint, type check, production build, sidebar route inspection |
| A-026 | FRD §5 KPIs 17, 21, 27 and 29 | The FRD does not define a canonical event for non-working time, whether “crane hours” are already summed, an OSBD cargo unit, or total equipment hours. | KPI-17/18 use `All Fast→Last Line Untied − Cargo Start→Cargo End`; crane-hour KPIs use `working_hours × resources_deployed`; KPI-27 requires an explicit cargo-unit cohort; and KPI-29 uses documented productive working hours plus documented downtime. Each returns UNAVAILABLE when its explicit denominator/input is absent. | Yes | `kpi.v2.*` formula registry | `test_kpi_engine.py` |
| A-027 | FRD §5 KPI 41 | The prescribed litigation/repair exclusions require source disposition flags that are not present in the governed workbook/model. | Compute the observed `Breakwater Out − Anchorage Arrival` population, retain the required exclusion fields in the KPI definition, and disclose that none were source-applied. Do not infer exclusions from duration, DQ status, or delay reason. When a source supplies the flags, add a versioned exclusion predicate. | Yes | `KPI-41` formula v2.1 | `test_kpi_engine.py`, validation harness |
| A-027 | FRD §§4.2–4.4, 9.3 | The source contract carries submission, request, schedule and served timestamps, but no separate service-end timestamp or explicit “Berth Commencement” event. | Expose Planning Lead Time (`requested − submission`), Scheduling Gap (`scheduled − requested`) and Execution Delay (`served − scheduled`) independently. Preserve negative execution delay as EARLY; flag schedule-before-request as DQ. Service Duration is `UNAVAILABLE` with its required missing input rather than inferred from delay. “Berthing Service” therefore exposes service scheduling/execution timing only; it does not silently claim physical berth-commencement delay. SHIFTING is retained as a third movement leg. | Yes | `analytics.service_timing.formula_version` | `test_service_timing_preserves_frd_delay_signs_and_legs`, `test_service_timing_missing_inputs_and_schedule_chronology_are_explicit` |
| A-028 | FRD ingestion / dataset replacement | A dataset group may contain separate operational workbooks plus harmless supplemental workbooks that contain no governed worksheet. | Discover governed worksheets per file. A workbook that parses successfully but contains none is retained in the immutable file manifest as `SKIPPED` and contributes no raw, staging, canonical, or analytical rows. Corrupt workbooks and malformed governed worksheets remain fatal. The group must contain at least one governed worksheet and continues to require VesselCalls under the existing minimum-dataset rule. Exact duplicate governed rows are retained in raw/staging and marked `DUPLICATE`; non-exact overlapping calls continue through the identity engine. | Yes | `ingestion.max_workbooks`, `ingestion_storage_path` | `test_mixed_group_skips_non_governed_workbook`, `test_non_governed_only_group_fails`, `test_fatal_file_schema_failure_keeps_group_inactive`, isolated master/split equivalence acceptance |
| A-029 | FRD §§4.2–4.5 DQ / audit | The FRD calls the operational action “delete”, while §2 requires immutable raw records and governed lineage. | The action is implemented as `EXCLUDED` on the selected staging row, never a physical deletion. Its source file/sheet/row remains immutable, an append-only audit event captures actor/reason/before/after, and the current dataset is rebuilt from non-excluded rows. Excluding a VesselCalls row may cause dependent source rows to be quarantined as orphans; this is disclosed by the resulting DQ records rather than silently cascading a delete. | No (governed) | `staging.record.validation_status=EXCLUDED` | `test_excluded_staging_rows_are_not_committed_to_canonical` |
| A-030 | §2 tenant/data-scope controls | Seeded authentication used `tenant-synthetic-01`, while legacy demo ingestion rewrote it to `synthetic-tenant`. | The authenticated principal is the sole tenant authority. The canonical governed demo/test tenant is `tenant-synthetic-01`; runtime aliases and mismatching client tenant overrides are prohibited. Migration `p4q5r6s7t8u9` normalizes the known legacy demo rows without changing record identity or lineage and adds tenant ownership to statistics/KPI persistence. | Yes (server-owned development identity only) | `development_tenant_id` | `test_tenant_identity.py`, `test_governed_two_file_acceptance_exposes_service_timings_and_statistics` |
| A-031 | FRD §§4.3–4.4 delay analysis | “Towage Wait” is requested without a separate tug-arrival or service-start field in the governed Services source. | Define Towage Wait as `Served_Time − Requested_Time` for Tug/Towage service records. Keep it separate from Scheduling Gap (`Scheduled − Requested`) and Tug/Towage Execution Delay (`Served − Scheduled`). Physical service-duration ranges use explicit service-specific canonical event pairs: Pilotage is `PILOT_ON_BOARD_ARRIVAL → PILOT_DISEMBARK_ARRIVAL`; Berthing is `FIRST_LINE_TIED_ARRIVAL → ALL_FAST_ARRIVAL`. Towage remains `UNAVAILABLE` because `TUG_SERVICE_END_ARRIVAL` is absent; All Fast is not substituted as a proxy. | No (source-driven) | `service-delay-overview-v1.0`, `service-duration-range-v1.1` | `test_service_delay_overview_and_duration_ranges_are_governed_and_unfiltered` |
| A-032 | FRD v2 §4.2.1 | The revised dictionary is wider than the current workbook and includes repeated resource slots and optional port-specific attributes. | Keep core vessel fields typed as columns, use canonical event occurrences for timestamped events, and preserve optional event/cargo attributes in governed JSON envelopes with raw cell lineage. Registry entries remain `NO_SOURCE_DATA` until a source supplies them. | Yes | `config/frd_v2_data_dictionary.yaml` | `test_v2_data_dictionary_is_complete_and_mappable` |
| A-033 | FRD v2 §4.4.3 | “Total Events” in Delay Frequency could include services lacking a schedule or actual timestamp. | Eligible events require both Scheduled Service Time and Actual Service Time; delayed means signed Execution Delay `> 0`. Exact 40% and 60% values are WATCH, below 40% is ACCEPTABLE, above 60% is CRITICAL. | No (explicit v2 semantics) | `service-delay-frequency-v2.0` | `test_v2_delay_frequency_boundaries_and_leg_populations` |
| A-034 | FRD v2 §4.4.6 | Several sample exception scenarios require no numeric standard beyond the default “unusually high/low” language. | Use linear-interpolated P90 for unusually high and P30 for unusually low. Explicit P95 and mean+3 sample-standard-deviations override the defaults. Rules with missing weather, tide, availability, clearance, speed, crane, resource-capacity or service-standard inputs remain registered as `NO_SOURCE_DATA`; no proxy is inferred. Comparable vessel-size and cargo-volume scenarios remain `UNAVAILABLE` until governed cohort bands are configured. | Yes | `config/outlier_rules.yaml` | `test_v2_outlier_registry_and_threshold_methods` |
